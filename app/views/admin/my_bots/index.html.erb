<% @page_title = "Mes Bots" %>

<h1 style="margin-bottom: 32px;"><i class="fa-icon fa-md fa-robot"></i> Mes Bots de Trading</h1>

<% if flash[:notice] %>
  <div class="bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 text-green-800 dark:text-green-200 px-4 py-3 rounded-lg mb-4">
    <%= flash[:notice] %>
  </div>
<% end %>
<% if flash[:alert] %>
  <div class="bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 text-red-800 dark:text-red-200 px-4 py-3 rounded-lg mb-4">
    <%= flash[:alert] %>
  </div>
<% end %>


        <% if @purchases.any? %>
          <% @purchases.each do |purchase| %>
            <% bot = purchase.trading_bot %>
            <div class="my-bot-card" onclick="window.location.href='<%= admin_bot_path(bot, from: 'my_bots') %>'" style="cursor: pointer;">
              <div class="bot-header">
                <div class="bot-info">
             
                  <div class="bot-details">
                    <h3><%= bot.name %></h3>
                    <div>
                      <span class="bot-status-badge" style="background: <%= bot.risk_badge_color %>; color: white;">
                        <%= bot.risk_label %>
                      </span>
                      <span class="bot-status-badge" style="background: <%= purchase.status_color %>; color: white;">
                        <%= purchase.status_badge %>
                      </span>
                    </div>
                  </div>
                </div>
                
                <% if current_user.is_admin? %>
                  <div class="bot-controls" onclick="event.stopPropagation();">
                    <%= button_to toggle_status_admin_my_bot_path(purchase), method: :post, 
                        class: "toggle-btn #{purchase.is_running? ? 'toggle-btn-stop' : 'toggle-btn-start'}" do %>
                      <% if purchase.is_running? %>
                        <i class="fa-icon fa-md fa-pause"></i> Arrêter
                      <% else %>
                        <i class="fa-icon fa-md fa-play"></i> Démarrer
                      <% end %>
                    <% end %>
                  </div>
                <% else %>
                  <div style="text-align: right;">
                    <div style="font-size: 13px; color: var(--text-muted); margin-bottom: 8px;">
                      Gestion du bot
                    </div>
                    <div style="font-size: 12px; padding: 8px 16px; background: var(--bg-hover); border-radius: 8px;">
                      Contactez l'admin
                    </div>
                  </div>
                <% end %>
              </div>
              
              <div class="bot-stats-grid">
                <div class="bot-stat-box">
                  <div class="bot-stat-label">Profit Total</div>
                  <div class="bot-stat-value <%= purchase.total_profit > 0 ? 'profit-positive' : 'profit-negative' %>">
                    <%= number_to_currency(purchase.total_profit, unit: "€", format: "%n %u") %>
                  </div>
                </div>
                
                <div class="bot-stat-box">
                  <div class="bot-stat-label">Trades</div>
                  <div class="bot-stat-value" style="color: var(--color-accent);">
                    <%= purchase.trades_count %>
                  </div>
                </div>
                
                <div class="bot-stat-box">
                  <div class="bot-stat-label">ROI</div>
                  <div class="bot-stat-value <%= purchase.roi_percentage > 0 ? 'profit-positive' : 'profit-negative' %>">
                    <%= number_to_percentage(purchase.roi_percentage, precision: 1) %>
                  </div>
                </div>
                
                <div class="bot-stat-box">
                  <div class="bot-stat-label">Jours Actifs</div>
                  <div class="bot-stat-value" style="color: var(--text-primary);">
                    <%= purchase.days_active %>
                  </div>
                </div>
              </div>
              
              <div style="background: var(--bg-hover); padding: 16px; border-radius: 12px; margin-bottom: 16px;">
                <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
                  <span style="font-size: 13px; font-weight: 600; color: var(--text-secondary);">
                    Drawdown : <%= number_to_percentage(purchase.drawdown_percentage, precision: 1) %>% / <%= number_to_percentage(bot.max_drawdown_limit, precision: 1) %>%
                  </span>
                  <span style="font-size: 13px; font-weight: 700; color: <%= purchase.within_drawdown_limit? ? 'var(--color-success)' : 'var(--color-danger)' %>;">
                    <%= purchase.drawdown_percentage.round(1) %>%
                  </span>
                </div>
                <div class="progress-bar">
                  <div class="progress-fill" style="width: <%= [purchase.drawdown_percentage, 100].min %>%; background: <%= purchase.within_drawdown_limit? ? 'linear-gradient(90deg, var(--color-success), var(--color-accent))' : 'var(--color-danger)' %>;"></div>
                </div>
                <% unless purchase.within_drawdown_limit? %>
                  <div style="margin-top: 8px; font-size: 12px; color: var(--color-danger); font-weight: 600;">
                    <i class="fa-icon fa-md fa-exclamation-triangle"></i> Attention : Limite de drawdown dépassée !
                  </div>
                <% end %>
              </div>
              
              <div style="display: flex; gap: 12px;">
                <div style="flex: 1; background: var(--bg-hover); padding: 12px; border-radius: 8px;">
                  <div style="font-size: 12px; color: var(--text-muted); margin-bottom: 4px;">Prix d'Achat</div>
                  <div style="font-size: 18px; font-weight: 700;"><%= number_to_currency(purchase.price_paid, unit: "€", format: "%n %u") %></div>
                </div>
                <div style="flex: 1; background: var(--bg-hover); padding: 12px; border-radius: 8px;">
                  <div style="font-size: 12px; color: var(--text-muted); margin-bottom: 4px;">Date d'Achat</div>
                  <div style="font-size: 18px; font-weight: 700;"><%= purchase.created_at.strftime("%d/%m/%Y") %></div>
                </div>
                <% if purchase.started_at %>
                  <div style="flex: 1; background: var(--bg-hover); padding: 12px; border-radius: 8px;">
                    <div style="font-size: 12px; color: var(--text-muted); margin-bottom: 4px;">
                      <%= purchase.is_running? ? 'Démarré le' : 'Arrêté le' %>
                    </div>
                    <div style="font-size: 18px; font-weight: 700;">
                      <%= (purchase.is_running? ? purchase.started_at : purchase.stopped_at)&.strftime("%d/%m/%Y") %>
                    </div>
                  </div>
                <% end %>
              </div>
              
              <div style="margin-top: 20px; padding-top: 16px; border-top: 1px solid var(--border-color); text-align: center;" onclick="event.stopPropagation();">
                <%= link_to admin_bot_path(bot, from: 'my_bots'), class: "btn", style: "background: linear-gradient(135deg, var(--color-accent), var(--color-accent-hover)); color: white; padding: 10px 32px; font-size: 14px; text-decoration: none; display: inline-block;" do %>
                  <i class="fa-icon fa-md fa-eye"></i> Voir les Détails Complets
                <% end %>
              </div>
            </div>
          <% end %>
        <% else %>
          <div class="card empty-state">
            <div class="empty-state-icon"><i class="fa-icon fa-md fa-robot"></i></div>
            <h2 style="margin: 0 0 12px 0; color: var(--text-primary);">Aucun Bot Pour le Moment</h2>
            <p style="margin: 0 0 24px 0; color: var(--text-muted);">
              Découvrez nos bots de trading dans la boutique
            </p>
            <%= link_to "<i class='fa-icon fa-md fa-shopping-cart'></i> Voir la Boutique".html_safe, admin_shop_index_path, class: "btn btn-success", style: "font-size: 16px; padding: 14px 32px;" %>
          </div>
        <% end %>