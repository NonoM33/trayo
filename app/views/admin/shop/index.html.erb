<!DOCTYPE html>
<html>
<head>
  <title>Boutique - Trayo</title>
  <%= stylesheet_link_tag "admin" %>
  <style>
    .shop-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
      gap: 1.5rem;
      margin-top: 1.5rem;
    }
    
    @media (min-width: 1920px) {
      .shop-grid {
        grid-template-columns: repeat(4, 1fr);
        gap: 2rem;
      }
    }
    
    @media (min-width: 1440px) and (max-width: 1919px) {
      .shop-grid {
        grid-template-columns: repeat(3, 1fr);
        gap: 1.75rem;
      }
    }
    
    @media (min-width: 1024px) and (max-width: 1439px) {
      .shop-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 1.5rem;
      }
    }
    
    @media (min-width: 769px) and (max-width: 1023px) {
      .shop-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 1.25rem;
      }
    }
    
    @media (min-width: 600px) and (max-width: 768px) {
      .shop-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
      }
    }
    
    @media (max-width: 599px) {
      .shop-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
      }
    }
    
    .bot-card {
      background: hsl(var(--card));
      border: 1px solid hsl(var(--border));
      border-radius: var(--radius);
      overflow: hidden;
      transition: all 0.15s;
    }
    
    .bot-card:hover {
      border-color: hsl(var(--muted-foreground) / 0.3);
    }
    
    .bot-card-image {
      width: 100%;
      height: 180px;
      background: hsl(var(--muted));
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 4rem;
      border-bottom: 1px solid hsl(var(--border));
    }
    
    .bot-card-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    
    .bot-card-badge {
      position: absolute;
      top: 0.75rem;
      right: 0.75rem;
      padding: 0.25rem 0.75rem;
      border-radius: 9999px;
      font-size: 0.75rem;
      font-weight: 600;
      color: white;
      text-transform: uppercase;
      letter-spacing: 0.025em;
    }
    
    .bot-card-content {
      padding: 1.5rem;
    }
    
    .bot-card-header {
      margin-bottom: 1rem;
    }
    
    .bot-card-title {
      font-size: 1.125rem;
      font-weight: 700;
      margin: 0 0 0.5rem 0;
      color: hsl(var(--foreground));
      line-height: 1.3;
      letter-spacing: -0.025em;
    }
    
    .bot-card-tagline {
      font-size: 0.875rem;
      color: hsl(var(--muted-foreground));
      margin: 0;
      line-height: 1.5;
    }
    
    .bot-card-stats {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      margin: 1rem 0;
      padding: 1rem;
      background: hsl(var(--muted) / 0.3);
      border-radius: calc(var(--radius) - 2px);
      border: 1px solid hsl(var(--border));
    }
    
    .bot-stat {
      text-align: center;
    }
    
    .bot-stat-label {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: hsl(var(--muted-foreground));
      margin-bottom: 0.375rem;
      font-weight: 600;
    }
    
    .bot-stat-value {
      font-size: 1.25rem;
      font-weight: 700;
      color: hsl(var(--foreground));
      letter-spacing: -0.025em;
    }
    
    .bot-card-price {
      font-size: 2rem;
      font-weight: 800;
      color: hsl(var(--foreground));
      text-align: center;
      margin: 1rem 0;
      letter-spacing: -0.025em;
    }
    
    .bot-card-cta {
      width: 100%;
      padding: 0.625rem 1rem;
      font-size: 0.875rem;
      font-weight: 600;
      border-radius: calc(var(--radius) - 2px);
      border: none;
      cursor: pointer;
      transition: all 0.15s;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      text-decoration: none;
    }
    
    .bot-card-cta-primary {
      background: hsl(var(--primary));
      color: hsl(var(--primary-foreground));
    }
    
    .bot-card-cta-primary:hover {
      background: hsl(var(--primary) / 0.9);
    }
    
    .bot-card-cta-secondary {
      background: transparent;
      color: hsl(var(--foreground));
      border: 1px solid hsl(var(--border));
    }
    
    .bot-card-cta-secondary:hover {
      background: hsl(var(--accent));
    }
    
    .bot-card-cta-owned {
      background: hsl(142 76% 36%);
      color: white;
      cursor: default;
    }
    
    .shop-header {
      text-align: center;
      margin-bottom: 2.5rem;
    }
    
    .shop-header h1 {
      font-size: 2rem;
      font-weight: 700;
      margin: 0 0 0.75rem 0;
      color: hsl(var(--foreground));
      line-height: 1.2;
      letter-spacing: -0.025em;
    }
    
    .shop-header p {
      font-size: 0.875rem;
      color: hsl(var(--muted-foreground));
      margin: 0;
    }
    
    .bot-users-count {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      margin-top: 0.75rem;
      padding: 0.5rem 0.75rem;
      background: hsl(var(--muted) / 0.5);
      border-radius: calc(var(--radius) - 2px);
      font-size: 0.75rem;
      color: hsl(var(--muted-foreground));
      font-weight: 600;
      border: 1px solid hsl(var(--border));
    }
    
    @media (max-width: 768px) {
      .shop-header h1 {
        font-size: 1.5rem;
      }
      
      .bot-card-content {
        padding: 1.25rem;
      }
      
      .bot-card-image {
        height: 160px;
      }
    }
    
    @media (max-width: 390px) {
      .shop-header {
        margin-bottom: 1.5rem;
      }
      
      .shop-header h1 {
        font-size: 1.375rem;
      }
      
      .shop-header p {
        font-size: 0.8125rem;
      }
      
      .bot-card-content {
        padding: 1rem;
      }
      
      .bot-card-image {
        height: 140px;
        font-size: 3rem;
      }
      
      .bot-card-title {
        font-size: 1rem;
      }
      
      .bot-card-tagline {
        font-size: 0.8125rem;
      }
      
      .bot-card-stats {
        gap: 0.75rem;
        padding: 0.875rem;
      }
      
      .bot-stat-label {
        font-size: 0.6875rem;
      }
      
      .bot-stat-value {
        font-size: 1.125rem;
      }
      
      .bot-card-price {
        font-size: 1.75rem;
      }
      
      .bot-card-cta {
        padding: 0.75rem 1rem;
        font-size: 0.9375rem;
        min-height: 48px;
      }
      
      .bot-users-count {
        font-size: 0.6875rem;
        padding: 0.5rem;
      }
    }
    
    @media (hover: none) and (pointer: coarse) {
      .bot-card-cta {
        min-height: 48px;
        font-size: 0.9375rem;
      }
      
      .bot-card {
        transition: none;
      }
      
      .bot-card:active {
        transform: scale(0.98);
      }
    }
  </style>
</head>
<body>
  <% @page_title = "Boutique" %>
  
  <div class="app-layout">
    <%= render 'admin/shared/sidebar' %>
    
    <div class="main-content">
      <%= render 'admin/shared/navbar' %>
      
      <div class="container" style="max-width: 1600px;">
        <% if flash[:notice] %>
          <div class="notice"><%= flash[:notice] %></div>
        <% end %>
        <% if flash[:alert] %>
          <div class="alert"><%= flash[:alert] %></div>
        <% end %>

        <div class="shop-header">
          <h1>Bots de Trading</h1>
          <p>Automatisez vos trades et maximisez vos profits</p>
        </div>

        <% if @bots.any? %>
          <div class="shop-grid">
            <% @bots.each do |bot| %>
              <div class="bot-card">
                <div class="bot-card-image">
                  <% if bot.image_url.present? %>
                    <img src="<%= bot.image_url %>" alt="<%= bot.name %>">
                  <% else %>
                    <div>🤖</div>
                  <% end %>
                  <div class="bot-card-badge" style="background: <%= bot.risk_badge_color %>;">
                    <%= bot.risk_label %>
                  </div>
                </div>
                
                <div class="bot-card-content">
                  <div class="bot-card-header">
                    <h3 class="bot-card-title"><%= bot.name %></h3>
                    <p class="bot-card-tagline"><%= bot.marketing_tagline %></p>
                  </div>
                  
                  <div class="bot-card-stats">
                    <div class="bot-stat">
                      <div class="bot-stat-label">Gain Annuel</div>
                      <div class="bot-stat-value">
                        <%= number_to_currency(bot.projection_yearly, unit: "€", format: "%n %u", precision: 0) %>
                      </div>
                    </div>
                    <div class="bot-stat">
                      <div class="bot-stat-label">Taux Réussite</div>
                      <div class="bot-stat-value">
                        <%= number_to_percentage(bot.win_rate, precision: 0) %>
                      </div>
                    </div>
                  </div>
                  
                  <div class="bot-card-price">
                    <%= bot.formatted_price %>
                  </div>
                  
                  <% if @my_bot_ids.include?(bot.id) %>
                    <%= link_to admin_my_bots_path, class: "bot-card-cta bot-card-cta-owned" do %>
                      Déjà Possédé
                    <% end %>
                  <% else %>
                    <%= link_to admin_shop_path(bot), class: "bot-card-cta bot-card-cta-secondary" do %>
                      Voir les Détails
                    <% end %>
                  <% end %>
                  
                  <% if bot.active_users_count > 0 %>
                    <div class="bot-users-count">
                      <span><%= bot.active_users_count %> <%= bot.active_users_count == 1 ? 'trader utilise' : 'traders utilisent' %> ce bot</span>
                    </div>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="card" style="text-align: center; padding: 4rem 2rem;">
            <div style="font-size: 4rem; margin-bottom: 1.5rem; color: hsl(var(--muted-foreground));">🤖</div>
            <h2 style="margin: 0 0 0.75rem 0; font-size: 1.5rem; font-weight: 700; letter-spacing: -0.025em;">Aucun Bot Disponible</h2>
            <p style="margin: 0; color: hsl(var(--muted-foreground)); font-size: 0.875rem;">Revenez bientôt pour découvrir nos bots de trading</p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</body>
</html>
