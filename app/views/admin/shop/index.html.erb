<!DOCTYPE html>
<html>
<head>
  <title>Boutique - Trayo</title>
  <%= stylesheet_link_tag "admin" %>
  <style>
    .shop-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
      gap: 1.5rem;
      margin-top: 1.5rem;
    }
    
    @media (min-width: 1920px) {
      .shop-grid {
        grid-template-columns: repeat(4, 1fr);
        gap: 2rem;
      }
    }
    
    @media (min-width: 1440px) and (max-width: 1919px) {
      .shop-grid {
        grid-template-columns: repeat(3, 1fr);
        gap: 1.75rem;
      }
    }
    
    @media (min-width: 1024px) and (max-width: 1439px) {
      .shop-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 1.5rem;
      }
    }
    
    @media (min-width: 769px) and (max-width: 1023px) {
      .shop-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 1.25rem;
      }
    }
    
    @media (min-width: 600px) and (max-width: 768px) {
      .shop-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
      }
    }
    
    @media (max-width: 599px) {
      .shop-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
      }
    }
    
    .bot-card {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: var(--radius-lg);
      overflow: hidden;
      transition: all 0.15s;
      box-shadow: var(--shadow-sm);
    }
    
    .bot-card:hover {
      border-color: var(--color-accent);
      box-shadow: var(--shadow-md);
      transform: translateY(-4px);
    }
    
    .bot-card-image {
      position: relative;
      width: 100%;
      height: 180px;
      background: var(--bg-hover);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 4rem;
      border-bottom: 1px solid var(--border-color);
    }
    
    .bot-card-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    
    .bot-card-badge {
      position: absolute;
      top: 0.75rem;
      right: 0.75rem;
      padding: 0.25rem 0.75rem;
      border-radius: 9999px;
      font-size: 0.75rem;
      font-weight: 600;
      color: white;
      text-transform: uppercase;
      letter-spacing: 0.025em;
    }
    
    .bot-card-content {
      padding: 1.5rem;
    }
    
    .bot-card-header {
      margin-bottom: 1rem;
    }
    
    .bot-card-title {
      font-size: 1.125rem;
      font-weight: 700;
      margin: 0 0 0.5rem 0;
      color: var(--text-primary);
      line-height: 1.3;
      letter-spacing: -0.025em;
    }
    
    .bot-card-tagline {
      font-size: 0.875rem;
      color: var(--text-muted);
      margin: 0;
      line-height: 1.5;
    }
    
    .bot-card-stats {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      margin: 1rem 0;
      padding: 1rem;
      background: var(--bg-hover);
      border-radius: var(--radius-md);
      border: 1px solid var(--border-color);
    }
    
    .bot-stat {
      text-align: center;
    }
    
    .bot-stat-label {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--text-muted);
      margin-bottom: 0.375rem;
      font-weight: 600;
    }
    
    .bot-stat-value {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--text-primary);
      letter-spacing: -0.025em;
    }
    
    .bot-card-price {
      font-size: 2rem;
      font-weight: 800;
      color: var(--text-primary);
      text-align: center;
      margin: 1rem 0;
      letter-spacing: -0.025em;
    }
    
    .bot-card-cta {
      width: 100%;
      padding: 0.625rem 1rem;
      font-size: 0.875rem;
      font-weight: 600;
      border-radius: var(--radius-md);
      border: none;
      cursor: pointer;
      transition: all 0.15s;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      text-decoration: none;
    }
    
    .bot-card-cta-primary {
      background: var(--color-accent);
      color: white;
    }
    
    .bot-card-cta-primary:hover {
      background: var(--color-accent-hover);
    }
    
    .bot-card-cta-secondary {
      background: transparent;
      color: var(--text-primary);
      border: 1px solid var(--border-color);
    }
    
    .bot-card-cta-secondary:hover {
      background: var(--bg-hover);
    }
    
    .bot-card-cta-owned {
      background: hsl(142 76% 36%);
      color: white;
      cursor: default;
    }
    
    .shop-header {
      text-align: center;
      margin-bottom: 2.5rem;
    }
    
    .shop-header h1 {
      font-size: 2rem;
      font-weight: 700;
      margin: 0 0 0.75rem 0;
      color: var(--text-primary);
      line-height: 1.2;
      letter-spacing: -0.025em;
    }
    
    .shop-header p {
      font-size: 0.875rem;
      color: var(--text-muted);
      margin: 0;
    }
    
    .bot-users-count {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      margin-top: 0.75rem;
      padding: 0.5rem 0.75rem;
      background: var(--bg-hover);
      border-radius: var(--radius-md);
      font-size: 0.75rem;
      color: var(--text-muted);
      font-weight: 600;
      border: 1px solid var(--border-color);
    }
    
    @media (max-width: 768px) {
      .shop-header h1 {
        font-size: 1.5rem;
      }
      
      .bot-card-content {
        padding: 1.25rem;
      }
      
      .bot-card-image {
        height: 160px;
      }
    }
    
    @media (max-width: 390px) {
      .shop-header {
        margin-bottom: 1.5rem;
      }
      
      .shop-header h1 {
        font-size: 1.375rem;
      }
      
      .shop-header p {
        font-size: 0.8125rem;
      }
      
      .bot-card-content {
        padding: 1rem;
      }
      
      .bot-card-image {
        height: 140px;
        font-size: 3rem;
      }
      
      .bot-card-title {
        font-size: 1rem;
      }
      
      .bot-card-tagline {
        font-size: 0.8125rem;
      }
      
      .bot-card-stats {
        gap: 0.75rem;
        padding: 0.875rem;
      }
      
      .bot-stat-label {
        font-size: 0.6875rem;
      }
      
      .bot-stat-value {
        font-size: 1.125rem;
      }
      
      .bot-card-price {
        font-size: 1.75rem;
      }
      
      .bot-card-cta {
        padding: 0.75rem 1rem;
        font-size: 0.9375rem;
        min-height: 48px;
      }
      
      .bot-users-count {
        font-size: 0.6875rem;
        padding: 0.5rem;
      }
    }
    
    @media (hover: none) and (pointer: coarse) {
      .bot-card-cta {
        min-height: 48px;
        font-size: 0.9375rem;
      }
      
      .bot-card {
        transition: none;
      }
      
      .bot-card:active {
        transform: scale(0.98);
      }
    }
  </style>
</head>
<body>
  <% @page_title = "Boutique" %>
  
  <div class="app-layout">
    <%= render 'admin/shared/sidebar' %>
    
    <div class="main-content">
      <%= render 'admin/shared/navbar' %>
      
      <div class="container" style="max-width: 1600px;">
        <% if flash[:notice] %>
          <div class="notice"><%= flash[:notice] %></div>
        <% end %>
        <% if flash[:alert] %>
          <div class="alert"><%= flash[:alert] %></div>
        <% end %>

        <div class="shop-header">
          <h1>Bots de Trading</h1>
          <p>Automatisez vos trades et maximisez vos profits</p>
        </div>

        <% if @bots.any? %>
          <div class="shop-grid">
            <% @bots.each do |bot| %>
              <div class="bot-card">
                <div class="bot-card-content">
                  <div class="bot-card-header" style="position: relative;">
                    <div style="display: flex; align-items: center; gap: 12px; margin-bottom: 12px;">
                     
                      <div style="flex: 1;">
                        <h3 class="bot-card-title" style="margin: 0 0 4px 0;"><%= bot.name %></h3>
                        <div style="display: flex; gap: 6px; flex-wrap: wrap;">
                          <span class="risk-badge risk-<%= bot.risk_level.downcase %>" style="font-size: 10px; padding: 4px 8px;">
                            <%= bot.risk_label %>
                          </span>
                          <% if @my_bot_ids.include?(bot.id) %>
                            <span style="background: linear-gradient(135deg, #10b981, #059669); color: white; padding: 4px 8px; border-radius: 12px; font-size: 10px; font-weight: 700; display: flex; align-items: center; gap: 4px;">
                              <i class="fa-icon fa-xs fa-check-circle"></i> Installé
                            </span>
                          <% end %>
                        </div>
                      </div>
                    </div>
                    <p class="bot-card-tagline"><%= bot.real_marketing_tagline %></p>
                  </div>
                  
                  <div class="bot-card-stats">
                    <div class="bot-stat">
                      <div class="bot-stat-label">Gain Annuel</div>
                      <div class="bot-stat-value">
                        <% 
                          real_projections = bot.calculate_real_projections
                          yearly_value = real_projections[:yearly] > 0 ? real_projections[:yearly] : bot.projection_yearly
                        %>
                        <%= number_to_currency(yearly_value, unit: "€", format: "%n %u", precision: 0) %>
                      </div>
                    </div>
                    <div class="bot-stat">
                      <div class="bot-stat-label">Taux Réussite</div>
                      <div class="bot-stat-value">
                        <% win_rate = bot.calculate_global_win_rate %>
                        <%= number_to_percentage(win_rate > 0 ? win_rate : bot.win_rate, precision: 0) %>
                      </div>
                    </div>
                  </div>
                  
                  <div class="bot-card-price">
                    <%= bot.formatted_price %>
                  </div>
                  
                  <% if @my_bot_ids.include?(bot.id) %>
                    <%= link_to admin_my_bots_path, class: "bot-card-cta bot-card-cta-secondary" do %>
                      <i class="fa-icon fa-md fa-arrow-right"></i> Voir mes bots
                    <% end %>
                  <% else %>
                    <%= link_to "Voir les Détails", admin_shop_path(bot), class: "bot-card-cta bot-card-cta-primary" %>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="card" style="text-align: center; padding: 4rem 2rem;">
            <div style="margin-bottom: 1.5rem; color: var(--text-muted);">
              <i class="fa-icon fa-md fa-robot"></i>
            </div>
            <h2 style="margin: 0 0 0.75rem 0; font-size: 1.5rem; font-weight: 700; letter-spacing: -0.025em;">Aucun Bot Disponible</h2>
            <p style="margin: 0; color: var(--text-muted); font-size: 0.875rem;">Revenez bientôt pour découvrir nos bots de trading</p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</body>
</html>
