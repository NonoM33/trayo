<!DOCTYPE html>
<html>
<head>
  <title><%= @product.name %> - Boutique Trayo</title>
  <%= stylesheet_link_tag "admin" %>
  <style>
    .product-page {
      max-width: 900px;
      margin: 0 auto;
      padding: 2rem;
    }
    
    .back-link {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      color: var(--text-muted);
      text-decoration: none;
      font-size: 0.9375rem;
      margin-bottom: 2rem;
      transition: color 0.2s;
    }
    
    .back-link:hover {
      color: var(--text-primary);
    }
    
    .product-hero {
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.15) 0%, rgba(5, 150, 105, 0.08) 100%);
      backdrop-filter: blur(40px);
      border: 2px solid rgba(16, 185, 129, 0.3);
      border-radius: 28px;
      padding: 3rem;
      margin-bottom: 2rem;
      position: relative;
      overflow: hidden;
    }
    
    .product-hero::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -20%;
      width: 60%;
      height: 200%;
      background: radial-gradient(circle, rgba(16, 185, 129, 0.1) 0%, transparent 70%);
      pointer-events: none;
    }
    
    <% if @product.badge.present? %>
    .product-badge {
      position: absolute;
      top: 1.5rem;
      right: 1.5rem;
      padding: 0.5rem 1.25rem;
      border-radius: 9999px;
      font-size: 0.8125rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      background: linear-gradient(135deg, #10b981, #059669);
      color: white;
      box-shadow: 0 4px 16px rgba(16, 185, 129, 0.4);
      animation: pulse-badge 2s infinite;
    }
    <% end %>
    
    @keyframes pulse-badge {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }
    
    .product-icon-large {
      width: 96px;
      height: 96px;
      border-radius: 24px;
      background: linear-gradient(135deg, #10b981, #059669);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.5rem;
      color: white;
      margin-bottom: 1.5rem;
      box-shadow: 0 12px 32px rgba(16, 185, 129, 0.35);
    }
    
    .product-title {
      font-size: 2.25rem;
      font-weight: 800;
      color: var(--text-primary);
      margin: 0 0 0.75rem 0;
      letter-spacing: -0.025em;
    }
    
    .product-description {
      font-size: 1.125rem;
      color: var(--text-muted);
      margin: 0;
      line-height: 1.6;
      max-width: 600px;
    }
    
    .product-details {
      display: grid;
      grid-template-columns: 1fr 380px;
      gap: 2rem;
    }
    
    @media (max-width: 768px) {
      .product-details {
        grid-template-columns: 1fr;
      }
    }
    
    .features-section {
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.04) 100%);
      backdrop-filter: blur(40px);
      border: 1px solid rgba(255, 255, 255, 0.15);
      border-radius: 20px;
      padding: 2rem;
    }
    
    .features-title {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--text-primary);
      margin: 0 0 1.5rem 0;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    
    .features-title i {
      color: #10b981;
    }
    
    .features-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    
    .features-list li {
      display: flex;
      align-items: flex-start;
      gap: 1rem;
      padding: 1rem 0;
      border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    }
    
    .features-list li:last-child {
      border-bottom: none;
    }
    
    .features-list .check-icon {
      width: 28px;
      height: 28px;
      border-radius: 8px;
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.2), rgba(16, 185, 129, 0.1));
      display: flex;
      align-items: center;
      justify-content: center;
      color: #10b981;
      font-size: 0.875rem;
      flex-shrink: 0;
    }
    
    .features-list .feature-text {
      font-size: 1rem;
      color: var(--text-secondary);
      line-height: 1.5;
    }
    
    .purchase-card {
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.12) 0%, rgba(5, 150, 105, 0.06) 100%);
      backdrop-filter: blur(40px);
      border: 2px solid rgba(16, 185, 129, 0.25);
      border-radius: 24px;
      padding: 2rem;
      position: sticky;
      top: 2rem;
    }
    
    .price-display {
      text-align: center;
      margin-bottom: 2rem;
      padding-bottom: 2rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .price-amount {
      font-size: 3.5rem;
      font-weight: 800;
      color: #10b981;
      letter-spacing: -0.025em;
      line-height: 1;
    }
    
    .price-interval {
      font-size: 1.25rem;
      color: var(--text-muted);
      font-weight: 500;
      margin-top: 0.5rem;
    }
    
    .price-note {
      font-size: 0.875rem;
      color: var(--text-muted);
      margin-top: 1rem;
    }
    
    .purchase-btn {
      width: 100%;
      padding: 1.25rem 2rem;
      font-size: 1.125rem;
      font-weight: 700;
      border-radius: 14px;
      border: none;
      cursor: pointer;
      transition: all 0.2s;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.75rem;
      text-decoration: none;
      background: linear-gradient(135deg, #10b981, #059669);
      color: white;
      box-shadow: 0 6px 24px rgba(16, 185, 129, 0.35);
      margin-bottom: 1rem;
    }
    
    .purchase-btn:hover {
      background: linear-gradient(135deg, #059669, #047857);
      box-shadow: 0 8px 32px rgba(16, 185, 129, 0.45);
      transform: translateY(-2px);
    }
    
    .purchase-btn-owned {
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.2), rgba(5, 150, 105, 0.1));
      color: #10b981;
      border: 2px solid rgba(16, 185, 129, 0.3);
      cursor: default;
      box-shadow: none;
    }
    
    .purchase-btn-owned:hover {
      transform: none;
      box-shadow: none;
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.2), rgba(5, 150, 105, 0.1));
    }
    
    .security-badges {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1.5rem;
      margin-top: 1.5rem;
      padding-top: 1.5rem;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .security-badge {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.8125rem;
      color: var(--text-muted);
    }
    
    .security-badge i {
      color: #10b981;
    }
    
    .guarantee-box {
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.02) 100%);
      border-radius: 12px;
      padding: 1rem;
      margin-top: 1.5rem;
      text-align: center;
    }
    
    .guarantee-box p {
      margin: 0;
      font-size: 0.875rem;
      color: var(--text-muted);
    }
    
    .guarantee-box strong {
      color: var(--text-primary);
    }
    
    .canceled-banner {
      background: linear-gradient(135deg, rgba(239, 68, 68, 0.2), rgba(220, 38, 38, 0.1));
      border: 1px solid rgba(239, 68, 68, 0.3);
      border-radius: 12px;
      padding: 1rem 1.5rem;
      margin-bottom: 2rem;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    
    .canceled-banner i {
      color: #ef4444;
    }
    
    .canceled-banner p {
      margin: 0;
      color: var(--text-primary);
      font-size: 0.9375rem;
    }
  </style>
</head>
<body>
  <% @page_title = @product.name %>
  
  <div class="app-layout">
    <%= render 'admin/shared/sidebar' %>
    
    <div class="main-content">
      <%= render 'admin/shared/navbar' %>
      
      <div class="product-page">
        <%= link_to admin_shop_index_path, class: "back-link" do %>
          <i class="fa-solid fa-arrow-left"></i>
          Retour à la Boutique
        <% end %>
        
        <% if params[:canceled] == 'true' %>
          <div class="canceled-banner">
            <i class="fa-solid fa-times-circle"></i>
            <p>Le paiement a été annulé. Vous pouvez réessayer quand vous le souhaitez.</p>
          </div>
        <% end %>
        
        <div class="product-hero">
          <% if @product.badge.present? %>
            <div class="product-badge"><%= @product.badge %></div>
          <% end %>
          
          <div class="product-icon-large">
            <i class="fa-solid <%= @product.icon %>"></i>
          </div>
          
          <h1 class="product-title"><%= @product.name %></h1>
          <p class="product-description"><%= @product.description %></p>
        </div>
        
        <div class="product-details">
          <div class="features-section">
            <h2 class="features-title">
              <i class="fa-solid fa-check-double"></i>
              Ce qui est inclus
            </h2>
            
            <ul class="features-list">
              <% @product.features_list.each do |feature| %>
                <li>
                  <div class="check-icon">
                    <i class="fa-solid fa-check"></i>
                  </div>
                  <span class="feature-text"><%= feature %></span>
                </li>
              <% end %>
            </ul>
          </div>
          
          <div class="purchase-card">
            <div class="price-display">
              <div class="price-amount"><%= @product.price.to_i %>€</div>
              <div class="price-interval"><%= @product.interval_label %></div>
              <% if @product.subscription? %>
                <p class="price-note">Renouvellement automatique, annulable à tout moment</p>
              <% end %>
            </div>
            
            <% if @already_owned %>
              <div class="purchase-btn purchase-btn-owned">
                <i class="fa-solid fa-check-circle"></i>
                Déjà souscrit
              </div>
            <% else %>
              <%= button_to purchase_admin_shop_product_path(@product), method: :post, class: "purchase-btn" do %>
                <i class="fa-brands fa-stripe"></i>
                Payer avec Stripe
              <% end %>
            <% end %>
            
            <div class="security-badges">
              <div class="security-badge">
                <i class="fa-solid fa-lock"></i>
                <span>Paiement sécurisé</span>
              </div>
              <div class="security-badge">
                <i class="fa-solid fa-shield-halved"></i>
                <span>SSL 256-bit</span>
              </div>
            </div>
            
            <div class="guarantee-box">
              <p><strong>Satisfait ou remboursé</strong> pendant 14 jours</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>

