<!DOCTYPE html>
<html lang="fr" class="dark">
<head>
  <title>Trayo - Connexion</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          colors: {
            neutral: {
              50: '#fafafa',
              100: '#f5f5f5',
              200: '#e5e5e5',
              300: '#d4d4d4',
              400: '#a3a3a3',
              500: '#737373',
              600: '#525252',
              700: '#404040',
              800: '#262626',
              900: '#171717',
              950: '#0a0a0a',
            }
          },
          fontFamily: {
            sans: ['Inter', 'system-ui', 'sans-serif'],
          },
        }
      }
    }
  </script>
  
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  
  <style>
    body {
      font-family: 'Inter', system-ui, sans-serif;
    }
    
    .gradient-orb {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      opacity: 0.4;
      animation: float 20s ease-in-out infinite;
    }
    
    .orb-1 {
      width: 400px;
      height: 400px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      top: -100px;
      right: -100px;
      animation-delay: 0s;
    }
    
    .orb-2 {
      width: 300px;
      height: 300px;
      background: linear-gradient(135deg, #3b82f6 0%, #06b6d4 100%);
      bottom: -50px;
      left: -50px;
      animation-delay: -5s;
    }
    
    .orb-3 {
      width: 200px;
      height: 200px;
      background: linear-gradient(135deg, #10b981 0%, #3b82f6 100%);
      top: 50%;
      left: 30%;
      animation-delay: -10s;
    }
    
    @keyframes float {
      0%, 100% { 
        transform: translate(0, 0) scale(1);
      }
      25% {
        transform: translate(30px, -30px) scale(1.05);
      }
      50% { 
        transform: translate(-20px, 20px) scale(0.95);
      }
      75% {
        transform: translate(20px, 10px) scale(1.02);
      }
    }
    
    .glass-card {
      background: rgba(23, 23, 23, 0.7);
      backdrop-filter: blur(24px);
      -webkit-backdrop-filter: blur(24px);
      border: 1px solid rgba(255, 255, 255, 0.08);
    }
    
    .input-field {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      transition: all 0.2s ease;
    }
    
    .input-field:focus {
      background: rgba(255, 255, 255, 0.08);
      border-color: #667eea;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.2);
      outline: none;
    }
    
    .input-field::placeholder {
      color: #737373;
    }
    
    .btn-gradient {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      transition: all 0.2s ease;
    }
    
    .btn-gradient:hover {
      background: linear-gradient(135deg, #5a6ce0 0%, #6a3fa0 100%);
      transform: translateY(-1px);
      box-shadow: 0 8px 24px rgba(102, 126, 234, 0.35);
    }
    
    .btn-gradient:active {
      transform: translateY(0);
    }
    
    .logo-icon {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      box-shadow: 0 8px 24px rgba(102, 126, 234, 0.3);
    }
    
    .grid-pattern {
      background-image: 
        linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
      background-size: 50px 50px;
    }
  </style>
</head>

<body class="min-h-screen bg-neutral-950 text-white antialiased overflow-hidden relative">
  <div class="absolute inset-0 grid-pattern"></div>
  
  <div class="gradient-orb orb-1"></div>
  <div class="gradient-orb orb-2"></div>
  <div class="gradient-orb orb-3"></div>
  
  <div class="relative z-10 min-h-screen flex items-center justify-center p-4">
    <div class="w-full max-w-md">
      <div class="glass-card rounded-2xl p-8 md:p-10 shadow-2xl">
        <div class="text-center mb-8">
          <div class="logo-icon w-16 h-16 rounded-2xl mx-auto mb-6 flex items-center justify-center">
            <span class="text-2xl font-bold text-white">T</span>
          </div>
          
          <h1 class="text-2xl md:text-3xl font-bold text-white mb-2 tracking-tight">
            Bienvenue sur Trayo
          </h1>
          <p class="text-neutral-400 text-sm md:text-base">
            Connectez-vous à votre espace de trading
          </p>
        </div>

        <% if flash[:alert] %>
          <div class="flex items-center gap-3 p-4 mb-6 rounded-xl bg-red-500/10 border border-red-500/20 text-red-400">
            <i class="fa-solid fa-circle-exclamation text-lg"></i>
            <span class="text-sm font-medium"><%= flash[:alert] %></span>
          </div>
        <% end %>

        <%= form_with url: admin_login_path, method: :post, class: "space-y-5" do |f| %>
          <div>
            <label for="email" class="block text-sm font-medium text-neutral-300 mb-2">
              <i class="fa-solid fa-envelope text-neutral-500 mr-2"></i>
              Email
            </label>
            <%= email_field_tag :email, nil, 
                id: "email",
                required: true, 
                placeholder: "votre@email.com",
                class: "input-field w-full px-4 py-3 rounded-xl text-white text-sm font-medium" %>
          </div>

          <div>
            <label for="password" class="block text-sm font-medium text-neutral-300 mb-2">
              <i class="fa-solid fa-lock text-neutral-500 mr-2"></i>
              Mot de passe
            </label>
            <%= password_field_tag :password, nil, 
                id: "password",
                required: true, 
                placeholder: "••••••••",
                class: "input-field w-full px-4 py-3 rounded-xl text-white text-sm font-medium" %>
          </div>

          <button type="submit" class="btn-gradient w-full py-3.5 rounded-xl text-white font-semibold text-sm flex items-center justify-center gap-2 mt-6">
            <i class="fa-solid fa-right-to-bracket"></i>
            Se connecter
          </button>
        <% end %>

        <div class="mt-8 pt-6 border-t border-white/5 text-center">
          <p class="text-neutral-500 text-xs">
            Trading automatisé • Bots MT5 • Performance
          </p>
        </div>
      </div>

      <div class="mt-6 text-center">
        <p class="text-neutral-600 text-xs">
          © <%= Date.current.year %> Trayo. Tous droits réservés.
        </p>
      </div>
    </div>
  </div>

  <div class="fixed bottom-6 left-6 hidden md:flex items-center gap-3 text-neutral-600 text-xs">
    <div class="flex items-center gap-1.5">
      <span class="w-2 h-2 bg-emerald-500 rounded-full animate-pulse"></span>
      <span>Système opérationnel</span>
    </div>
  </div>

  <div class="fixed bottom-6 right-6 hidden md:flex items-center gap-4">
    <a href="#" class="text-neutral-600 hover:text-neutral-400 transition-colors text-xs">
      Support
    </a>
    <a href="#" class="text-neutral-600 hover:text-neutral-400 transition-colors text-xs">
      Conditions
    </a>
  </div>
</body>
</html>
