<!DOCTYPE html>
<html>
<head>
  <title>Credits - Admin Trayo</title>
  <%= stylesheet_link_tag "admin" %>
  <style>
    .credits-layout {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 32px;
      margin-bottom: 32px;
    }
    
    .credits-stats {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }
    
    .credits-table-card {
      grid-column: 1 / -1;
    }
    
    .stat-card {
      background: var(--bg-card);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border-color);
      padding: 24px;
      text-align: center;
      transition: all var(--transition-fast);
      position: relative;
      overflow: hidden;
    }
    
    .stat-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 4px;
      background: var(--gradient-accent);
    }
    
    .stat-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-lg);
      border-color: var(--color-accent);
    }
    
    .stat-icon {
      font-size: 32px;
      margin-bottom: 12px;
      display: block;
    }
    
    .stat-label {
      font-size: 12px;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.8px;
      margin-bottom: 8px;
      font-weight: 700;
    }
    
    .stat-value {
      font-size: 24px;
      font-weight: 800;
      color: var(--text-primary);
      line-height: 1.2;
    }
    
    .stat-value.positive {
      color: var(--color-success);
    }
    
    .credits-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 24px;
    }
    
    .credits-header h1 {
      font-size: 28px;
      font-weight: 800;
      margin: 0;
      background: var(--gradient-accent);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    .credits-summary {
      background: var(--bg-info-box);
      border-radius: var(--radius-lg);
      padding: 24px;
      margin-bottom: 32px;
      border-left: 4px solid var(--color-info);
      border: 2px solid var(--color-info);
    }
    
    .credits-summary h3 {
      color: var(--color-info);
      font-size: 18px;
      margin: 0 0 16px 0;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    .credits-summary p {
      margin: 0;
      color: var(--text-primary);
      font-size: 15px;
      line-height: 1.6;
    }
    
    @media (min-width: 1920px) {
      .credits-layout {
        grid-template-columns: 1fr 1fr;
        gap: 40px;
      }
      
      .credits-stats {
        grid-template-columns: repeat(4, 1fr);
        gap: 24px;
      }
      
      .stat-card {
        padding: 32px;
      }
      
      .stat-icon {
        font-size: 40px;
      }
      
      .stat-value {
        font-size: 28px;
      }
    }
    
    @media (min-width: 1440px) and (max-width: 1919px) {
      .credits-layout {
        grid-template-columns: 1fr 1fr;
        gap: 32px;
      }
      
      .credits-stats {
        grid-template-columns: repeat(4, 1fr);
        gap: 20px;
      }
    }
    
    @media (min-width: 1025px) and (max-width: 1439px) {
      .credits-layout {
        grid-template-columns: 1fr;
        gap: 24px;
      }
      
      .credits-stats {
        grid-template-columns: repeat(2, 1fr);
        gap: 16px;
      }
    }
    
    @media (max-width: 1024px) {
      .credits-layout {
        grid-template-columns: 1fr;
        gap: 20px;
      }
      
      .credits-stats {
        grid-template-columns: repeat(2, 1fr);
        gap: 12px;
      }
      
      .stat-card {
        padding: 20px;
      }
      
      .stat-icon {
        font-size: 28px;
      }
      
      .stat-value {
        font-size: 20px;
      }
    }
    
    @media (max-width: 768px) {
      .credits-stats {
        grid-template-columns: 1fr;
        gap: 12px;
      }
      
      .credits-header {
        flex-direction: column;
        align-items: stretch;
        gap: 16px;
      }
      
      .credits-header h1 {
        font-size: 24px;
      }
      
      .stat-card {
        padding: 16px;
      }
      
      .stat-icon {
        font-size: 24px;
      }
      
      .stat-value {
        font-size: 18px;
      }
    }
  </style>
</head>
<body>
  <% @page_title = "Credits Management" %>
  
  <div class="app-layout">
    <%= render 'admin/shared/sidebar' %>
    
    <div class="main-content">
      <%= render 'admin/shared/navbar' %>
      
      <div class="container">
        <% if flash[:notice] %>
          <div class="notice"><%= flash[:notice] %></div>
        <% end %>
        <% if flash[:alert] %>
          <div class="alert"><%= flash[:alert] %></div>
        <% end %>
        
        <div class="credits-header">
          <h1>Credits Management</h1>
          <div class="action-buttons">
            <%= link_to "View Clients", admin_clients_path, class: "btn btn-primary" %>
          </div>
        </div>
        
        <div class="credits-summary">
          <h3>📊 Overview</h3>
          <p>Manage and track all credit transactions for your clients. Credits can be added manually or automatically through various system processes.</p>
        </div>
        
        <div class="credits-layout">
          <div class="credits-stats">
            <div class="stat-card">
              <span class="stat-icon">💳</span>
              <div class="stat-label">Total Credits</div>
              <div class="stat-value"><%= @credits ? @credits.count : 0 %></div>
            </div>
            <div class="stat-card">
              <span class="stat-icon">💰</span>
              <div class="stat-label">Total Amount</div>
              <div class="stat-value positive">
                <%= @credits ? number_to_currency(@credits.sum(&:amount), unit: "€", format: "%n %u") : "€0.00" %>
              </div>
            </div>
            <div class="stat-card">
              <span class="stat-icon">📈</span>
              <div class="stat-label">Average Credit</div>
              <div class="stat-value">
                <%= @credits && @credits.any? ? number_to_currency(@credits.sum(&:amount) / @credits.count, unit: "€", format: "%n %u") : "€0.00" %>
              </div>
            </div>
            <div class="stat-card">
              <span class="stat-icon">👥</span>
              <div class="stat-label">Active Clients</div>
              <div class="stat-value">
                <%= @credits ? @credits.map(&:user_id).uniq.count : 0 %>
              </div>
            </div>
          </div>
          
          <div class="credits-table-card">
            <div class="card">
              <h2>All Credits</h2>
              
              <% if @credits && @credits.any? %>
                <div class="table-wrapper">
                  <table>
                    <thead>
                      <tr>
                        <th>Date</th>
                        <th>Client</th>
                        <th>Amount</th>
                        <th>Reason</th>
                        <th>Actions</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% @credits.each do |credit| %>
                        <tr>
                          <td><%= credit.created_at.strftime("%Y-%m-%d %H:%M") %></td>
                          <td>
                            <%= link_to credit.user.email, admin_client_path(credit.user), style: "color: var(--color-accent); text-decoration: none;" %>
                          </td>
                          <td class="positive"><%= number_to_currency(credit.amount, unit: "€", format: "%n %u") %></td>
                          <td><%= credit.reason || "—" %></td>
                          <td>
                            <%= link_to "View Client", admin_client_path(credit.user), class: "btn btn-primary btn-sm" %>
                          </td>
                        </tr>
                      <% end %>
                    </tbody>
                  </table>
                </div>
              <% else %>
                <div style="text-align: center; padding: 60px 20px;">
                  <div style="font-size: 48px; margin-bottom: 20px; color: var(--text-muted);">💳</div>
                  <h3 style="margin: 0 0 12px 0; font-size: 20px; font-weight: 700; color: var(--text-primary);">No Credits Yet</h3>
                  <p style="margin: 0; color: var(--text-muted); font-size: 14px;">Start by adding credits to your clients</p>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>

